# HTTP Configuration (HTTPS disabled for initial deployment)
# To enable HTTPS later, see 05-https.config.disabled

option_settings:
  # HTTP Listener only (no SSL required)
  aws:elbv2:listener:80:
    Protocol: HTTP
    DefaultProcess: default

  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx

